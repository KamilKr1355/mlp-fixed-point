[project]
name = "mlp-fixed-point"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "tabulate>=0.10.0",
    "torch>=2.12.0",
    "torchvision>=0.27.0",
]
